// Re-export from plugins for backward compatibility
export {
  savePaymentDocument,
  saveImage,
  saveDocument,
  saveSampleAttachment,
  deleteUploadedFile,
  getRelativePath,
} from '../plugins/fileUpload.js';
